<UserControl x:Class="AdventOfCode2020.Views.DayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdventOfCode2020.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Choose the day and press run"/>
        <TextBox Grid.Row="1" Text="{Binding DayId}" Margin="0 5 0 0"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 5 0 0">
            <Button Content="Run" Command="{Binding RunDailyInputCommand}"/>
            <Button Content="Test" Margin="5 0 0 0" Command="{Binding RunDailyUnitTestCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
